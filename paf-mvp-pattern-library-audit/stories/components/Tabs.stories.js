import TabsComponent from './Tabs';

export default {
};

export const Tabs = TabsComponent.bind({});
Tabs.args = {
  tabs: [
    'Your random ID',
    'Your marketing preferences',
    'Sync ID'
  ]
};
